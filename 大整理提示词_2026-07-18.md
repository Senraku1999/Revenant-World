# 大整理提示词包（生成于 2026-07-18 深夜）

目标：对项目文档/脚本与本地记忆做一次冗余清退、过期更新、一致性校准。**不含**角色卡内容创作（T1-C 六篇开场白、T2 批次 31 项是另一条线，勿混入）。

**用法**：按阶段顺序逐条粘贴给 Claude，每阶段结束核对"完成判据"后再发下一条。中途出问题粘贴【回调】提示词。全部完成后本文件可删。

---

## 阶段 0 · 安全基线（先跑这条）

```
开始大整理前先建立回滚锚点：
1. 确认 git 工作区干净、与 origin/main 同步，记录当前 HEAD 短哈希作为回滚锚点，写给我看。
2. 备份记忆目录：把 memory 目录整体复制一份为同级的 memory_backup_20260719，确认文件数一致。
3. 只做以上两件事，不动任何其他文件。
完成判据：报出 HEAD 哈希 + 备份文件数比对结果。
```

---

## 阶段 1 · 盘点与计划（只读，不动文件）

```
对项目和记忆做只读盘点，产出整理计划，本阶段禁止修改任何文件：
1. 项目侧扫描范围：CLAUDE.md、.claude/（agents/workflows/CHEATSHEET/TOOLS_GUIDE/settings）、创作者文件/ 全部文档与脚本、根目录。找：与现行实现矛盾的文档、无人引用的死代码/死文档、同一信息的多处硬编码副本（如分配表、BASE_ENTRIES 在脚本与验证间重复）、Python 时代残留、失效路径引用。
2. 记忆侧扫描范围：memory 目录全部文件 + MEMORY.md 索引。找：合集与单条内容重叠、指向已不存在的文件/脚本/规范名的过期引用、与项目现状矛盾的断言、[[链接]]断链、索引与实际文件不符、"repo 已记录不该存记忆"的重复项、一次性任务型记忆是否已完成可归档。
3. 每个发现列成清单项：编号｜位置（文件:行）｜证据摘录｜问题类型（冗余/过期/矛盾/断链）｜建议处置（删/改/合并/存疑待问）｜风险（低/中/高）。
4. 输出为根目录《整理计划_20260719.md》，末尾单列"存疑待问"区。凭证据说话，不确定的一律进存疑区，不许猜。
完成判据：计划文件生成，我审阅后批准进入阶段 2。
```

---

## 阶段 2 · 交叉检查（把计划变成可执行清单）

```
基于我批准的《整理计划_20260719.md》做逐项核实：
1. 每个"删/改/合并"项重新打开原文核对证据仍成立，标记为"确认"；核对不上的降级进存疑区。
2. 高风险项（涉及脚本行为、多文件联动、记忆合集重组）逐项列出至少 2 个失败模式和缓解措施。
3. 存疑区所有问题一次性汇总向我提问，拿到答复前对应项冻结。
4. 把确认项分成执行批次：A 项目文档批 / B 项目脚本批 / C 记忆批，写回计划文件并标注批次。
约束：判断"文档 vs 实现"矛盾时以设计文档为真理源报缺陷，不推定文档过时（记忆 verification-independent-oracle）；看不懂为什么存在的东西不动，进存疑区（Chesterton's fence）。
完成判据：计划文件更新为带批次的执行清单，存疑项全部有我的答复。
```

---

## 阶段 3 · 分批执行（每批独立收口）

按 A→B→C 逐批发送，一批绿了再发下一批：

```
执行《整理计划_20260719.md》的 [A/B/C] 批：
1. 逐项执行，修改一律 Edit 逐条改（脚本只许只读扫描），每项完成在计划文件中打勾并记一行实际改动。
2. 批内全部完成后跑验证门：npx tsx "创作者文件/审查文件/自动检查.ts" 全过；若本批动了导出相关脚本或文档，加跑 npm run 全管线 + 按 pipeline-per-step-verification 记忆做三管线逐步重演验证；若是记忆批，逐文件核对 frontmatter 完整、[[链接]]可达、MEMORY.md 索引一行不多不少。
3. 验证全绿后向我报告本批清单与验证结果，经我确认后 git commit（记忆批无需 commit，报告即可）。
4. 执行中新发现的问题不现场扩大战场：记入计划文件"新增发现"区，留待批后处理。
完成判据：本批全项打勾 + 验证门全绿 + （项目批）提交完成。
```

---

## 阶段 4 · 收尾

```
大整理收尾：
1. 核对《整理计划_20260719.md》无未处理项（存疑区、新增发现区清零或有我的明确搁置指示）。
2. 更新 CLAUDE.md 与 MEMORY.md 中因本次整理而变化的引用。
3. 清理临时文件（含计划文件本身，问我是否保留）；确认记忆备份是否删除（默认保留一周）。
4. 输出总结：删了什么、改了什么、合并了什么、遗留什么，以及本次整理暴露的流程教训（值得的写入记忆）。
5. 经我确认后 push。
完成判据：工作区干净、与远端同步、总结报告输出。
```

---

## 【回调】异常处置（出问题时粘贴）

```
停止当前批次。执行回调流程：
1. 报告：哪一项、做了什么、观察到什么异常（原样贴错误输出，不解读不猜测）。
2. 回滚本批：项目文件 git checkout 恢复到批前状态；记忆文件从 memory_backup_20260719 按文件恢复。
3. 回滚后重跑验证门确认恢复干净。
4. 分析根因后给我两个以上备选方案再动手。同一问题两次修不好即放弃该项，标记"搁置"并说明原因。
```

## 【恢复】中断续做（新会话粘贴）

```
继续大整理：读根目录《整理计划_20260719.md》，报告当前进度（已完成批次、打勾项、存疑区状态），
确认 git 状态与最近 commit 对应到哪一批，然后从断点继续，不重做已打勾项。
```

---

## 附录 · 今晚已知线索（给明天省时间，非完整清单）

**记忆侧候选**：
- 合集 vs 单条重叠：`武器格式合集` vs `weapon-字段三分类`；`标点规则合集` vs `省略号引号内边界`/`比喻边界规则` 等
- `token-budget-2026-07` 内容已写入创作指导 §2.2（repo 已记录，可瘦身为指针）
- `2026-07-09-first-distortion-timeline-shift` 是一次性修改清单，若已全部落地可归档
- 各记忆引用的文件名需验存在性（py 清退、"世界书构建规范.md"改名类问题今晚已抓到一例）

**项目侧候选**：
- 三张分配表 + BASE_ENTRIES 分散硬编码在导出脚本中，无单一来源模块（评估是否值得抽取，属中风险项）
- `.claude/agents/`（13 个）与 `workflows/`、CHEATSHEET、TOOLS_GUIDE 是否与现行流程一致未核查过
- `宏观审查计划.md`、二级审查脚本与现行十段式口径的一致性
- `角色卡导出规范.md` 今晚刚全面同步过 TS 实现，属新鲜文档，重点核对我今晚的改法你是否认可

**明确不动**：角色卡内容（T1-C/T2 另行安排）、git 历史、node_modules、底图/、导出角色卡/。
