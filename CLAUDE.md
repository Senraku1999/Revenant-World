# CLAUDE.md

## 项目概述

狩灵世界观是以当代日本双重社会为背景的灵能题材角色扮演世界观。每个角色含三个文件：`{角色名}.json`（角色卡）、`{角色名}简介.md`、`{角色名}开场白.md`。

| 目录 | 内容 |
| ------ | ------ |
| `创作者文件/创作文件/` | 创作指导、格式标准 |
| `创作者文件/审查文件/标点审查/` | 三级标点审查规则与脚本 |
| `创作者文件/共享代码/` | 管线与审查脚本共享的 TS 模块 |
| `创作者文件/导出文件/world info/` | 世界观与规则条目（World Info） |
| `创作者文件/导出文件/关系网/` | 按阵营分目录的角色关系副本 |
| `创作者文件/导出文件/世界书/` | 按阵营分册的 ST 世界书 JSON |
| `角色卡/` | 全部角色卡（按阵营分目录） |
| `导出角色卡/` | 导出角色卡 |
| `小说化内容/` | 世界观叙事文学 |

---

## 一、核心规则

**以卡为准**：规范定义字段和格式，卡片定义实际写法。创作或修改字段值时，必须确认至少三张同类角色卡的同字段写作方式。规范与全体卡片不一致时，以卡片为准。

**标点**：中文遵循 GB/T 15834，数字用法遵循 GB/T 15835，异形词遵循 GF 1001—2001。英文遵循英文标准语法。以下约束中英文共同适用，符号形式按各自语言惯例（中文 `——` `……` `；` `：`，英文 `—` `…` `;` `:`）：

- 角色卡（JSON/简介/开场白）：中文叙述仅 `，` `。` `、` 三标点，`""` 为唯一合法引号，嵌套引号（`""` 内 `''`）合法。
- `；`/`;` 仅限并列关系；`——`/`—` 与 `……`/`…` 仅限对话内。中英文叙述中共同禁止。
- `：`/`:` 在枚举（`X：A、B、C`）、解释（`X：Y说明`）、引述（`说："..."`）中为合法用法，其余场景避免使用。
- 小说化内容：中国标准文学标点，中文引号 `""`，句尾 `？！` 解锁，`——` 和 `……` 在叙述中同样禁止（仅限对话内）。

标点审查三级体系：一级 GB/T 15834 → 二级项目追加收紧 → 三级三步法，详见 `创作者文件/审查文件/标点审查/`。审查脚本源码与规则文档中作为检测模式或示例的违禁字符（如曲引号 `“”`）为合法举例位：审查不判违规；执行全局字符替换前必须排除审查脚本、共享代码与规则文档路径。

**JSON**：遵循 RFC 8259。UTF-8 编码，无 BOM。

**ControlBoundary**：允许试探发问，禁止明确设定玩家身份、目的或关系。

**共享事件同步**：修改涉及多个角色的共享事件后，必须逐文件检查全部关联角色，确认时间锚点、责任归属、基本事实无矛盾。

---

## 二、创作

> **每次修改角色文件之前，以下两步不得跳过：**

**第一步：重读规范（禁止依赖会话记忆）。** 根据修改类型，用 Read 打开对应指导文件：

| 修改内容 | 必读文件 |
| ------ | ------ |
| JSON | `创作者文件/创作文件/狩灵角色卡创作指导.md`、`创作者文件/创作文件/角色卡标准格式.md` |
| 简介 | `创作者文件/创作文件/狩灵简介创作指导.md`、`创作者文件/创作文件/简介标准格式.md` |
| 开场白 | `创作者文件/创作文件/狩灵开场白创作指导.md` |
| 任何角色文件 | `创作者文件/导出文件/world info/宏观规则.md` |
| 导出/PNG/世界书/关系网 | `创作者文件/导出文件/角色卡导出规范.md` |
| appearance 字段 | `创作者文件/创作文件/狩灵角色卡创作指导.md` §2.1.1–2.1.3 |
| face 字段 | `创作者文件/创作文件/Face描述标准.md` |
| clothing 字段 | `创作者文件/创作文件/Clothing描述标准.md` |
| overview 字段 | `创作者文件/创作文件/Overview描述标准.md` |
| 术语/专有名词 | `创作者文件/创作文件/专有名词全称表.md` |
| 基础能力字段 | `创作者文件/创作文件/基础能力等级示例.md` |
| 灵力回路字段 | `创作者文件/创作文件/灵力回路描述标准.md` |

**第二步：修改角色文件后的验证门。** `npx tsx "创作者文件/审查文件/自动检查.ts"`（命令见第四节），全部通过为达标。

---

## 三、团队

Agent 配置文件位于 `.claude/agents/`。

### 项目经理

| Agent | 职责 |
| ------ | ------ |
| `pm-orchestrator` | 任务分解、agent 调度、进度追踪、结果汇总 |

### 学术团队

| Agent | 领域 |
| ------ | ------ |
| `anthropologist` | 文化系统、亲属关系、仪式、帮会亚文化、双重社会 |
| `geographer` | 城市分布、地盘空间、场景地理锚定、通勤逻辑 |
| `historian` | 时间线、事件因果、年代断代、组织起源 |
| `narratologist` | 叙事结构、开场白三段式、对话风格、角色 voice |
| `psychologist` | 人格张力、创伤处理、动机可信度、缺陷真实性 |

### 审查团队

| Agent | 职责 |
| ------ | ------ |
| `review-coordinator` | 流程管理、任务分配、结果汇总、最终裁决 |
| `punctuation-reviewer` | 按 GB/T 15834 + 项目规则，逐字标点扫描 |
| `logic-reviewer` | 跨文件/跨角色逻辑矛盾检测 |
| `boundary-reviewer` | ControlBoundary 边界审查 |
| `prose-reviewer` | 文笔审查 + 开场白六项机械核查（肢体/能力/武器/时间/空间/闭环） |

> **AI 味审查**：审查流程固定环节，使用 `humanizer-zh` skill 检测全项目/抽样角色的 AI 写作痕迹（29 类模式），按 T0/T1/T2 三级输出检测清单（仅检测不修复）。

全量审查（`full-review`）：全角色覆盖，无抽样。自动脚本 → 标点+逻辑并行 → AI 味审查 → 全角色扫描 → 学术五视角并行 → 汇总分级。

抽样审查（`sampling-review`）：轻量快速。自动脚本 → 标点+逻辑并行 → AI 味审查（统一抽样 8 角色）→ 学术五视角并行 → 汇总分级（含未抽样风险提示）。

> **审查后清理**：审查结束后删除所有临时文件（`*_output.txt`、`*_extract*.ts`、`cross_file_*`、`三级审查结果.txt`（汇总入报告后即删）、根目录下以 `_` 开头的 `.ts`/`.txt` 等）。永久脚本为 `自动检查.ts`、`一级审查.ts`、`三级审查.ts` 与三条导出管线脚本（`导出关系网.ts`、`导出世界书.ts`、`导出角色卡.ts`），统一使用 `npx tsx` 运行。

---

## 四、常用命令

```bash
# 审查
npx tsx "创作者文件/审查文件/自动检查.ts"               # 全项目自动扫描
npx tsx "创作者文件/审查文件/标点审查/一级审查/一级审查.ts"       # 一级国标标点审查
npx tsx "创作者文件/审查文件/标点审查/三级审查/三级审查.ts"       # 三级三步法
# 导出管线（按序运行）
npx tsx "创作者文件/导出文件/导出关系网.ts"              # 管线一：角色卡 → 关系网
npx tsx "创作者文件/导出文件/导出世界书.ts"              # 管线二：关系网 → 世界书
npx tsx "创作者文件/导出文件/导出角色卡.ts"              # 管线三：底图 + 世界书 → PNG
npm run 全管线                                           # 一键跑全管线
# 工具
node -e "JSON.parse(require('fs').readFileSync('文件.json','utf-8'))" > /dev/null
node -e "const f=require('fs');console.log(f.readdirSync('目录').length)"
# 内联脚本规则（本机 npx tsx -e 被 Windows npx.cmd 换行截断，禁止使用，特例也已被 settings 阻断）
node -e "内联纯 JS 脚本"                                       # 首选：CJS 包（js-tiktoken 等）直接 require
node node_modules/tsx/dist/cli.mjs -e "内联 TS 脚本"           # 需要导入项目 TS 模块时使用（绕开 npx.cmd，多行安全）
# 临时 TS 文件也是合法替代：Write → npx tsx 文件.ts → rm
```

---

## 五、代码规范

### 类型系统

- TypeScript 严格模式（`strict: true`），全项目统一。
- 禁止 `any`。不可控外部结构用 `unknown`，需使用时先做类型收窄。
- 共享类型定义集中在 `创作者文件/共享代码/types.ts`，各脚本通过 `import` 引用。

### 命名与格式

- 导出常量：全大写下划线（`UPPER_SNAKE_CASE`），如 `VALID_IDENTITY`、`EXCLUDE_DIRS`。
- 对象/字典的键值对：手动空格对齐，确保同组内 `:` 或 `:` 后的值列对齐。
- 代码区块使用 `// ── 区块名 ──` 视觉分隔，区块名简洁描述功能（如 `// ── 异形词加载 ──`）。

### 依赖管理

- 文件系统遍历、路径过滤、二进制解析：必须使用 Node.js 原生 API（`fs`、`path`、`Buffer`、位运算）。
- 禁止引入 `glob`、`fs-extra` 等第三方包替代现有手写底层实现。
- 项目唯一运行时依赖为 `js-tiktoken`（Token 计数）。新增依赖必须说明不可替代的理由并等待确认。

### 数据源解析

- 项目中 Markdown 文件可作为结构化数据源使用（如 `GF_1001_2001_异形词整理表.md` 被 `standards.ts` 解析）。
- 修改解析逻辑时保持现有的轻量级字符串处理模式（`split` 截取、正则匹配、`startsWith`/`includes` 过滤）。
- 禁止引入 AST 解析器，禁止强制要求将数据源转换为 JSON/YAML 格式。

### 架构边界

- 不同功能模块的数据表刻意保持独立（如 `RELATION_FACTION_MEMBERS` / `WORLDBOOK_BUILD` / `PNG_EXPORT_ASSIGN` 三张分配表）。
- 禁止以 DRY 或简化名义合并结构相似但服务于不同导出管线的配置项。跨模块的数据冗余是隔离业务影响的刻意设计。
- 带有明显防御意图的代码（显式校验、`existsSync` 守卫、`unknown` 替代不可控外部类型）不得以消除冗余为由移除，除非确认该防御对象已不存在。
