# CLAUDE.md

## 项目概述

狩灵世界观是以当代日本双重社会为背景的灵能题材角色扮演世界观。每个角色含三个文件：`{角色名}.json`（角色卡）、`{角色名}简介.md`、`{角色名}开场白.md`。

| 目录 | 内容 |
|------|------|
| `world info/` | 世界观、狩灵协会、灵能者、异常、评级、锈钟、宏观规则 |
| `创作者文件/` | 创作指导、格式标准、审查脚本 |
| `协会狩灵角色卡/` | 14 名 |
| `事务所狩灵角色卡/` | 7 名 |
| `工坊狩灵角色卡/` | 2 名 |
| `游魂角色卡/` | 4 名 |
| `罪灵角色卡/` | 3 名 |
| `锈钟角色卡/` | 6 名 |
| `事件卡/` | 3 张 |
| `世界观卡/` | 2 张 |
| `角色关系网/` | 全角色 35、晨昏事务所 5、花坂家 3、追猎 3、异常 2 |

---

## 一、核心规则

**标点**：中文遵循 GB/T 15834，英文遵循英文标准语法。项目附加两条硬规则：`""`（英文直双引号）为唯一合法引号，`——` `……` 仅限对话（引号内）使用。

**句号 vs 逗号判定**（基于 GB/T 15834 的句末/句中停顿区分，追加项目判例）：

逗号表示话未说完，句号表示一个完整意思结束。以下三种情况应用逗号而非句号：

1. **名词性标签后接展开**：`A。B。` 中 A 是身份/角色/能力标签而无谓语动词时，A 与 B 之间应用逗号。如 `晨昏事务所的防御专家，性格温和。`（非 `专家。性格温和。`）
2. **连续动作/事件共享主语**：同一主语的行为链中，句号切断动作连贯性。如 `持桂妮薇儿冲入战场，在决斗中轮换五德。`（非 `战场。在决斗中`）
3. **平行标签对举**：`X：…。Y：…` 中 X 与 Y 为同一维度下平行对等项时，应用分号连接。如 `月：冷酷、果断；明：怯懦、勤勉。`

能力描述中"概要→机制/效果"的展开、武器描述中"武器名→性能特征"的展开、起源中连续人生阶段的衔接，均适用以上规则。

**分号仅限并列关系**：分号连接的两个分句必须是同一维度下的平行对等项，互换位置不改变语义。因果、递进、转折、说明均非并列，应用逗号或句号。

**JSON**：遵循 RFC 8259。UTF-8 编码，无 BOM。

**角色独立性**：角色自身字段（overview、abilities、origin、mission）不提及偶发性关联的其他角色（如"为X打造了武器""由Y指导"）。定义性关联除外——若关系是角色存在的核心动机（如劫追杀慎），可在自身字段中出现。关联细节仅写在 `char_relationships` 中。

**外观描述**：只写可观察事实，禁用"像""仿佛""似乎"等比喻词。`face` 写脸型、五官、瞳色、肤色，`features` 写伤疤纹身等显著标记。

**ControlBoundary**：允许试探发问，禁止明确设定玩家身份、目的或关系。

**Token**：角色卡四段式 A[300-500] BC[0-500] D[200-400] 总[500-1400]，开场白[900-1100]，tiktoken cl100k_base。

**评级格式**：JSON `色彩阶`，简介 `色彩阶-称号`，带连字符。游魂 JSON `"None"` ↔ 简介 `"无"`。

其余已确认的判定边界及细则详见 `审查计划.md`。

---

## 二、创作

> **每次修改角色文件之前，以下两步不得跳过：**

**第一步：重读规范（禁止依赖会话记忆）。**

根据修改的文件类型，用 Read 工具打开对应指导文件过一遍，不做任何编辑，先读完：

| 修改内容 | 必读文件 |
|----------|----------|
| JSON | `创作者文件/狩灵角色卡创作指导.md`、`创作者文件/角色卡标准格式.md` |
| 简介 | `创作者文件/狩灵简介创作指导.md`、`创作者文件/简介标准格式.md` |
| 开场白 | `创作者文件/狩灵开场白创作指导.md` |
| 任何角色文件 | `world info/宏观规则.md` |

**第二步：修改后跑检查，全部达标再报告。**

```bash
python "创作者文件/审查脚本_自动检查.py"
```

任一检查未通过 → 继续修改 → 再跑检查 → 直到全部达标。不等用户提醒。

---

## 三、团队

本项目配备三支 AI 团队，通过 Agent 工具调用。agent 配置文件位于 `.claude/agents/`。

### 项目经理

| Agent 文件 | 职责 |
|-----------|------|
| `pm-orchestrator` | 任务分解、agent 调度、进度追踪、结果汇总。复杂多步骤任务时优先调用。 |

### 学术团队（创作/审查阶段通用）

| Agent 文件 | 领域 |
|-----------|------|
| `anthropologist` | 文化系统、亲属关系、仪式、帮会亚文化、双重社会 |
| `geographer` | 城市分布、地盘空间、场景地理锚定、通勤逻辑 |
| `historian` | 时间线、事件因果、年代断代、组织起源 |
| `narratologist` | 叙事结构、开场白三段式、对话风格、角色 voice |
| `psychologist` | 人格张力、创伤处理、动机可信度、缺陷真实性 |

### 审查团队（审查阶段）

| Agent 文件 | 职责 |
|-----------|------|
| `review-coordinator` | 流程管理、任务分配、结果汇总、最终裁决。不逐行审查。 |
| `punctuation-reviewer` | 按 GB/T 15834 + 项目两条硬规则，逐字标点扫描。 |
| `logic-reviewer` | 先聚合再比对，跨文件/跨角色逻辑矛盾检测。 |

审查按 `审查计划.md` 六步执行（读取规则 → 复述理解 → 基础格式层 → 逻辑一致性层 → 汇总分级 → 自查修正），详见 `审查方法论.md`。全量审查流程：pm-orchestrator 调度 → 自动脚本 → 标点审查 + 逻辑审查并行 →（深度审查可选：学术五视角并行）→ 汇总分级 → 自查。

---

## 四、常用命令

```bash
python "创作者文件/审查脚本_自动检查.py"          # 全项目自动扫描
python -m json.tool 文件.json > /dev/null           # JSON 语法检查
```
