export const meta = {
  name: 'full-review',
  description: '狩灵世界观全量审查：自动脚本 → 标点+逻辑并行 → 全角色学术五视角并行 → 汇总分级（全角色，无抽样）',
  phases: [
    { title: '自动脚本', detail: '运行自动检查脚本获取基线' },
    { title: '标点审查', detail: '逐字扫描所有 MD 文件标点合规性' },
    { title: '逻辑审查', detail: '聚合比对跨文件/跨角色一致性' },
    { title: '全角色扫描', detail: '列出全部角色目录，按阵营分组，供学术审查使用' },
    { title: '学术审查', detail: '五视角领域审查，覆盖全部角色' },
    { title: '汇总', detail: '整合结果，T0/T1/T2 分级输出' },
    { title: '清理', detail: '清除中间产物，仅保留最终报告' },
  ],
}

phase('自动脚本')

const rules = {
  CLAUDE_MD: await agent('读取 CLAUDE.md 的标点规则和团队结构部分，仅输出规则摘要。', {label: 'read-claude-md'}),
  审查计划: await agent('读取 创作者文件/审查文件/其他审查/宏观审查计划.md 全文，仅输出六步流程和分级标准。', {label: 'read-review-plan'}),
  审查方法论: await agent('读取 创作者文件/审查文件/其他审查/宏观审查方法论.md 全文，仅输出聚合比对原则。', {label: 'read-methodology'}),
}

log(`规则文件已读取。CLAUDE.md 标点硬规则："" 唯一引号，—— …… 仅限对话内。其余遵循 GB/T 15834。`)

phase('标点审查')
phase('逻辑审查')

const [punctuationResult, logicResult] = await Promise.all([
  agent(`
你是标点审查专员。按以下规则扫描项目中所有 *简介.md 和 *开场白.md 文件：

## 硬规则（必须逐字检查）
1. 引号必须是英文直双引号 ""（U+0022），禁止中文卷曲引号 ""
2. —— 仅限对话引号内，叙述中禁止
3. …… 仅限对话引号内，叙述中禁止
4. 无英文省略号 ...
5. 无 Markdown 装饰符（* _ ~）
6. 无星号包裹动作（*text*）

## 注意事项
- 冒号 : 和分号 ; 在 GB/T 15834 下合法，不要误判
- 但非言语动词后冒号接对话（如 抬头："）是 GB/T 15834 违规，用逗号
- JSON 非对话字段中的 —（em dash U+2014）禁止

扫描全部文件后，按文件夹分批输出问题。格式：【文件名】→ 问题类型 → 具体位置 → 严重等级（T0/T1/T2）
零问题文件汇总一行即可。
  `, {label: 'punctuation-review', phase: '标点审查'}),

  agent(`
你是逻辑一致性审查员。按"先聚合再比对"原则检查项目逻辑一致性。

## 前置阅读
必须读取：创作者文件/审查文件/其他审查/宏观审查方法论.md、创作者文件/审查文件/其他审查/宏观审查计划.md、创作者文件/导出文件/world info/ 全部文件、创作者文件/创作文件/角色卡标准格式.md、创作者文件/创作文件/基础能力等级示例.md

## 审查清单
### 4a. 同角色三文件交叉比对（JSON ↔ 简介 ↔ 开场白）
对所有角色检查：基础信息、基础能力、特殊能力、装备、性格特质、评级格式一致

### 4b. 跨角色矛盾检查
重点检查：关系双向一致性（花坂三兄妹、晨昏四成员、烬-劫-慎三角）
同一事件在不同角色卡中描述一致

### 4c. 世界观设定一致性
科室分配、评级体系、身份定义、char_faction/char_status 格式、评估方署名

### 4d. 示例对话与性格一致性
语言风格匹配、无 ControlBoundary 违规、动作描写多样化

输出格式：【角色A】与【角色B/规则】矛盾点 → 双方原文 → T0/T1/T2
零问题时声明"无矛盾"。
  `, {label: 'logic-review', phase: '逻辑审查'}),
])

log(`标点审查完成，逻辑审查完成`)

phase('全角色扫描')

const allCharacters = await agent(`
扫描以下六个角色目录，列出每个目录下的全部角色子目录名：

- 角色卡/协会狩灵角色卡/
- 角色卡/事务所狩灵角色卡/
- 角色卡/游魂角色卡/
- 角色卡/罪灵角色卡/
- 角色卡/锈钟角色卡/
- 角色卡/工坊狩灵角色卡/

对于每个角色子目录，确认其包含的三个文件（JSON、简介.md、开场白.md）是否存在。
如有缺失文件，标记出来。

输出格式：
===ALL_CHARACTERS===
## 协会狩灵角色卡（N名）
1. 角色名 — 文件完整/缺失X
2. 角色名 — 文件完整
...
（其余五个目录同理）
===END===
`, {label: 'character-scanner', phase: '全角色扫描'})

const totalCount = allCharacters ? allCharacters.match(/\d+\./g)?.length || 0 : 0
log(`全角色扫描完成：约 ${totalCount} 名角色，全部进入学术审查`)

phase('学术审查')

const academicResults = await Promise.all([
  agent(`
你是人类学家。读取 创作者文件/导出文件/world info/ 全部文件。

## 全角色审查名单（以下为全部角色，逐一审查，不得跳过）
${allCharacters}

## 审查要求
阅读以上每位角色的 JSON + 简介 + 开场白三个文件。从以下角度审查：
- 文化系统：角色是否体现了双重社会的运作逻辑（表/里社会切换、灵视广告发现机制）
- 帮会亚文化：阵营内部的文化特征是否一致（协会科层制、事务所市井气、锈钟松散联盟）
- 家族仪式：花坂血统相关的家族传统是否自洽
- 跨文化碰撞：多国籍/多文化背景角色的文化锚点是否立得住

按阵营分批输出。每阵营标注亮点和问题。问题标注 T1/T2，说明涉及角色和文件。
审查完毕后单独输出一份"审查覆盖率确认"：列出实际审查了的角色名，确保无遗漏。
  `, {label: 'anthropologist', phase: '学术审查'}),

  agent(`
你是地理学家。读取 创作者文件/导出文件/world info/狩灵协会.md 和 创作者文件/导出文件/world info/锈钟.md。

## 全角色审查名单（以下为全部角色，逐一审查，不得跳过）
${allCharacters}

## 审查要求
阅读以上每位角色的 JSON + 简介 + 开场白三个文件。从以下角度审查：
- 城市分布：协会科室/事务所/工坊的所在地是否合理（东京集中度、地方分布）
- 地盘空间：锈钟角色的地盘描述是否有地理冲突
- 场景锚定：开场白中的具体场景（车站、街道、建筑）地理逻辑是否自洽
- 通勤逻辑：角色在不同场景间的移动是否合理

按阵营分批输出。每阵营标注亮点和问题。问题标注 T1/T2，说明涉及角色和文件。
审查完毕后单独输出一份"审查覆盖率确认"：列出实际审查了的角色名，确保无遗漏。
  `, {label: 'geographer', phase: '学术审查'}),

  agent(`
你是历史学家。读取 创作者文件/导出文件/world info/ 全部文件和 事件卡/ 全部文件。

## 全角色审查名单（以下为全部角色，逐一审查，不得跳过）
${allCharacters}

## 审查要求
阅读以上每位角色的 JSON + 简介 + 开场白三个文件。从以下角度审查：
- 时间线：角色背景中的年代断代是否与世界观历史一致（花坂血统起源、协会建立、锈钟形成）
- 事件因果：同一事件在不同角色文件中的描述是否一致
- 组织起源：角色所属组织的成立时间、创始人、演变过程是否有矛盾
- 代际关系：不同年代角色（如漆黑构筑 vs 新世代）的时间锚点是否正确

按阵营分批输出。每阵营标注亮点和问题。问题标注 T1/T2，说明涉及角色和文件。
审查完毕后单独输出一份"审查覆盖率确认"：列出实际审查了的角色名，确保无遗漏。
  `, {label: 'historian', phase: '学术审查'}),

  agent(`
你是叙事学家。读取 创作者文件/创作文件/狩灵开场白创作指导.md。

## 全角色审查名单（以下为全部角色，逐一审查，不得跳过）
${allCharacters}

## 审查要求
阅读以上每位角色的开场白（及其 JSON 的对话示例）。从以下角度审查：
- 叙事结构：开场白是否遵循三段式（场景锚定→角色登场→对话钩子）
- 对话风格：角色 voice 是否与 char_personality 一致，不同角色间差异化是否足够
- 收尾台词：最后一句是否形成有效的 RP 入口（开放但不空洞）
- 节奏控制：叙述与对话的比例、段落长度、信息密度是否适宜

按阵营分批输出。每阵营标注亮点和问题。问题标注 T1/T2，说明涉及角色和文件。
审查完毕后单独输出一份"审查覆盖率确认"：列出实际审查了的角色名，确保无遗漏。
  `, {label: 'narratologist', phase: '学术审查'}),

  agent(`
你是心理学家。读取 创作者文件/导出文件/world info/异常.md。

## 全角色审查名单（以下为全部角色，逐一审查，不得跳过）
${allCharacters}

## 审查要求
阅读以上每位角色的 JSON + 简介 + 开场白三个文件。从以下角度审查：
- 人格张力：char_personality 中的矛盾特质是否有机整合（而非简单并列）
- 创伤处理：char_background 中的创伤事件是否在性格/行为中有合理投射
- 动机可信度：角色的核心动机是否与其背景、能力、处境逻辑一致
- 缺陷真实性：flaws/quirks 是否真正影响角色行为，而非"名义上的缺点"

按阵营分批输出。每阵营标注亮点和问题。问题标注 T1/T2，说明涉及角色和文件。
审查完毕后单独输出一份"审查覆盖率确认"：列出实际审查了的角色名，确保无遗漏。
  `, {label: 'psychologist', phase: '学术审查'}),
])

log(`五个学术视角全部返回`)

phase('汇总')

const report = await agent(`
你是审查协调员。汇总以下四路审查结果，按 T0/T1/T2 分级输出最终报告。

## 全角色审查名单
${allCharacters}

## 自动脚本结果
${JSON.stringify(rules)}

## 标点审查结果
${punctuationResult}

## 逻辑审查结果
${logicResult}

## 学术审查结果
人类学：${academicResults[0]}
地理学：${academicResults[1]}
历史学：${academicResults[2]}
叙事学：${academicResults[3]}
心理学：${academicResults[4]}

## 输出要求
1. 先列 T0（必须修复），再 T1（建议修复），最后 T2（可选优化）
2. 每项标注来源（脚本/标点/逻辑/学术-领域）
3. 给出修复优先级建议
4. 零问题时明确声明
5. 注意：本次为全角色审查，不包含抽样——不需要"未抽样角色风险提示"。全部角色均已覆盖。
`, {label: 'coordinator-summary', phase: '汇总'})

log('全量审查完成。提醒：清理创作者文件/审查文件/ 目录下的临时文件（_开头的中间产物）。')

return { report }
